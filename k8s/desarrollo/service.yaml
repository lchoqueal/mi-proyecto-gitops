apiVersion: v1
kind: Service
metadata:
  name: pedidos-api-service
  namespace: pedidos-env
spec:
  type: NodePort
  selector:
    app: pedidos-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30100
